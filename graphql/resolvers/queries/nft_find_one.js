export default function nft_find_one(_, query, { models }) { 
    const {
        id
    } = query

    //TO-DO

    return {
        mint_to:"mint_to",
        prompt:"prompt",
        minted:false,
        id:0
    }
}